метод ПоказатьПолучателейПисьма(ОбъектСписок: Объект?): Строка
    пер Результат = ""
    для Получатель из ОбъектСписок как Массив<Строка> 
        Результат += Получатель + ", "
    ;
    если Результат.Пусто()
        возврат ""
    ;
    возврат Результат.ПодстрокаСНачала(Результат.Длина() - 2)
;