@Обработчик
метод ПередЗаписью(ПредыдущееЗначение: КэшТокенов.Данные, ПараметрыЗаписи: КэшТокенов.ПараметрыЗаписи)
    пер СуществующаяЗапись: КэшТокенов.ДанныеТокена? = КэшТокенов.НайтиТокен(АдресСервера, ИдКлиента)
    если СуществующаяЗапись != Неопределено
        и СуществующаяЗапись.Ссылка != Ссылка 
        
        выбросить новый КэшТокенов.ИсключениеСохраненияТокена("Запись о токене уже существует")
    ;
;